sudo add-apt-repository -y ppa:snappy-dev/tools; and sudo apt -y -qq update; and sudo apt -y install \
    wget \
    curl \
    locate \
    unzip \
    vim \
    build-essential \
    snapcraft \
    expect \
    fonts-hack-ttf
