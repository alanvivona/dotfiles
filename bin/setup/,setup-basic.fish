sudo add-apt-repository -y ppa:snappy-dev/tools; and sudo apt -y -q update; and sudo apt -y install \
    wget \
    curl \
    locate \
    unzip \
    vim \
    build-essential \
    snapcraft \
    fonts-hack-ttf
