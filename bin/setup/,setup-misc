#!/usr/bin/env fish
sudo apt-get -y install \
    nload \
    htop \
    glances \
    slurm \
    jq \
    ddgr \ # duckduckgo cli

sudo npm install -g tldr vtop
sudo snap install fkill
sudo pip3 install asciinema
# sudo cargo install ripgrep # maybe?
pip3 install jupyter jupyterthemes --user
