#!/usr/bin/env fish
sudo apt -y update; and sudo apt -y install \
    apt-transport-https \
    wget \
    curl \
    locate \
    unzip \
    vim \
    build-essential \
    expect \
    fonts-hack-ttf
