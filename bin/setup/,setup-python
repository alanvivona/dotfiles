#!/usr/bin/env fish

# Python 3 + pip
sudo apt-get -y install python3-pip

python3 -m pip install --upgrade pip

sudo pip3 install --upgrade \
    autopep8 \
    pylint \
    prospector \
    pytest \
    nose \
    flake8 \
    ptpython 