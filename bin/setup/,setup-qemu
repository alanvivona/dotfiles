#!/usr/bin/env fish
sudo apt -y install qemu-system

# sudo qemu-system-x86_64 -hda debian.img -boot d -cdrom ~/Downloads/debian-9.6.0-amd64-netinst.iso -m 2048 -enable-kvm
# sudo qemu-system-x86_64 -hda debian.img -boot c -enable-kvm
